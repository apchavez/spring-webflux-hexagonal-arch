spring:
  r2dbc:
    url: r2dbc:h2:mem:///customer-db;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:
  sql:
    init:
      mode: always
  data:
    redis:
      host: localhost
      port: 6379

spring:
  kafka:
    producer:
      bootstrap-servers: localhost:9092

management:
  health:
    redis:
      enabled: false
