spring:
  r2dbc:
    url: r2dbc:h2:mem:///test-db;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:
  sql:
    init:
      mode: always
      data-locations: ""
  data:
    redis:
      host: localhost
      port: 6379

jwt:
  private-key-location: classpath:certs/private.pem

management:
  health:
    redis:
      enabled: false
