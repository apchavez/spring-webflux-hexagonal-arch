spring:
  flyway:
    locations: classpath:db/migration
  data:
    redis:
      host: localhost
      port: 6379

jwt:
  private-key-location: classpath:certs/private.pem

management:
  health:
    redis:
      enabled: false
