# Copy this file to .env and adjust values before running docker compose.
# The .env file is git-ignored (.gitignore) to avoid exposing credentials.

# PostgreSQL database
DB_NAME=customerdb
DB_USER=customer_user
DB_PASSWORD=change_me_in_production
